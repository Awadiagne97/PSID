language: java
sudo: true
dist: trusty
jdk: oraclejdk9
addons:
  apt:
    packages:
      - oracle-java9-installer
before_install:
  - chmod +x mvnw

services:
  - postgresql
before_script:
  - psql -c 'create test_postgres_db;' -U postgres
script : ./mvnw clean install
